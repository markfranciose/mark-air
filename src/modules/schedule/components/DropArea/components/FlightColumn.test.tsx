/* Moved the testing to the DropArea component, to avoid 
 * exquisite context stubbing */

import React from 'react';

test('renders static UI', () => {
  /*
    render(
      <FlightColumn flightData={recievedData}/>
    );
    const destinationText = screen.getByText(/ORD/i);
    expect(destinationText).toBeInTheDocument();
   */
});
